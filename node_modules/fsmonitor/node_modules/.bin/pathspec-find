#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../pathspec/bin/pathspec-find.js" "$@"
  ret=$?
else 
  node  "$basedir/../pathspec/bin/pathspec-find.js" "$@"
  ret=$?
fi
exit $ret
